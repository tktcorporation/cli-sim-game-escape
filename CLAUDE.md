# CLI Sim Game Escape

## Project Overview

Rust + WebAssembly で動くターミナルスタイルのブラウザゲームプラットフォーム。
Ratzilla (ratatui wrapper) を使用し、10 ticks/sec の固定タイムステップで動作する。

### Tech Stack
- Rust (Edition 2021), wasm32-unknown-unknown
- Ratzilla (ratatui-based terminal rendering to browser DOM)
- Trunk (build system)
- web-sys (browser APIs)

### Architecture
- `src/main.rs` — エントリポイント、メニュー、イベントループ
- `src/games/mod.rs` — Game trait (handle_input, tick, render)
- `src/games/cookie/` — Cookie Factory ゲーム
- `src/games/factory/` — Tiny Factory ゲーム
- `src/input.rs` — クリック/タップ座標変換
- `src/time.rs` — 固定タイムステップ (10 ticks/sec)

### Design Principles
- **Pure Logic Pattern**: logic.rs に純粋関数、state.rs にデータ、render.rs は読み取り専用
- **Trait-Based Games**: 新ゲーム追加時に main.rs の変更不要
- **Responsive**: 60列を閾値にワイド/ナローレイアウトを切り替え
- **Simulation-Driven Balance**: ゲーム開発・バランス調整は simulator.rs の自動プレイ
  シミュレーターとセットで行う（詳細は `.claude/rules/game-simulation.md`）
- 日本語 UI

### PR前チェックリスト
- `cargo test` — 全テスト通過を確認
- `cargo clippy -- -W clippy::all` — 警告ゼロを確認してからPRを出す
- dead_code警告が出たら `#[cfg(test)]` やフィールド削除で対応する

### Important: タッチイベント処理

**タッチ処理はJS側（index.html）で行う。Rust/WASMでは処理しない。**

理由: web-sysのTouchEvent/Touch/TouchList機能を使うと、iOS SafariのWebKit WASMエンジンが
黒画面になる問題がある（コミット 0caf982 で発見・修正）。

現在の実装:
- `index.html` 内のJavaScriptで `touchstart` イベントをリッスン
- クリックされた`<pre>`要素のテキストから `[X]` 形式のキーを正規表現で抽出
- `KeyboardEvent` を `dispatchEvent` してRatzillaの `on_key_event` ハンドラに渡す

正規表現 `/\[([0-9A-Za-z\-=!~{}|\\])\]/` がサポートするキー:
- 生産者: `[1]`〜`[9]`, `[0]`, `[-]`, `[=]`
- タブ: `[{]`, `[|]`, `[\]`, `[}]`, `[~]`
- 一括操作: `[!]`
- アルファベット: `[A]`〜`[Z]`

新しいキーを追加する場合は、この正規表現も更新すること。

二重発火防止は二重防御:
- JS 側で `touchstart` 内 `e.preventDefault()` によりブラウザの compatibility mouse event を抑制
- Rust 側で `ClickState` のタイムスタンプベース dedup（直近 30ms 以内の同セルタップを無視）

`dom_element_to_cell` が `<pre>` を特定できなかった場合は、`pixel_y_to_row` /
`pixel_x_to_col` のピクセル計算 fallback に切り替わる。観測のため失敗時には
`console.warn` を出すので、頻発するようなら DevTools で気付ける。

---

### Important: クリック処理アーキテクチャ規約

**1. クリック対象の登録は必ず widgets 経由で行う**

直接 `ClickState::add_click_target` / `add_row_target` を呼ぶのは widgets 内部のみ。
clippy の `disallowed_methods` で禁止済み（`clippy.toml` 参照）。アプリ／ゲーム
コードでは以下のいずれかの widget primitive を使うこと:

- `TabBar` — 横並びタブ
- `ClickableList` — 縦並びリスト
- `ClickableGrid` — 2D グリッド
- `Clickable` — 任意の `Widget` をフル領域でクリック可能にする
  （オーバーレイ・単発ボタン・スクロールインジケータなど）

「back ボタンが欲しい」「ダイアログのボタンが欲しい」と思った時は、`Clickable` で
既存の `Paragraph` / `Block` をラップするのが正解。新たに座標を手書きする必要はない。

**2. オーバーレイは別 DOM 要素を生やさない**

`dom_element_to_cell` は `elementFromPoint` で `<pre>` を特定する設計。back
ボタンや confirmation dialog などのオーバーレイは Ratzilla の同じ `<pre>` 上に
**上書き描画**する（`Paragraph` / `Block` で同じセルへ render）。

別 `<div>` / `<canvas>` / `<iframe>` を grid 上に重ねると `<pre>` ヒット検出が
壊れるので絶対に避けること。

**3. 新しい widget primitive を追加する時のチェックリスト**

- render と click target 登録を**同じメソッド呼び出し**に閉じ込める
- `area` の値はメソッド内で 1 度だけ参照する（ローカル変数で受け取って使い回さない）
- テストで `TestBackend` を使い、登録された `rect` が実描画位置と一致することを確認

新規ゲーム追加時に `#[allow(clippy::disallowed_methods)]` を増やす PR は基本
レビューで止まる。widget の方を拡張するか、既存 primitive で表現できないか検討。

---

## Game Design Concepts

### Cookie Factory — コアコンセプト: 「投資戦略」

**コア体験**: 「今この選択が正しいのか？」と考え、実際に効率的な判断ができた時の快感

**プレイヤーの感情**: 「Farmを今3台買うより、もう少し貯めてMineを1台買った方が回収が早い…！」

**主軸**: 複数の投資先から最適な選択を見極める戦略的判断

**デザイン原則**:
- すべての要素は「投資判断を面白くする」ために存在する
- 情報は隠さず見せる（CPS/コスト比率、回収時間など）— 戦略ゲームは情報が命
- 「明らかな最適解」を避け、状況によって最適な選択が変わるようにする
- 数字のインフレ自体は手段であり目的ではない — インフレは戦略の新しいフェーズを開くために使う

**現状の課題**:
- 生産者5種が単純な上位互換で、戦略的深さが薄い
- アップグレードは×2固定で、購入タイミングの判断余地がない
- ROI（投資回収率）が可視化されておらず、戦略を感覚でしか判断できない
- コストカーブが固定的で「今買うか貯めるか」のジレンマが弱い

**強化の方向性**:
- 投資効率の可視化（CPS/コスト、回収時間の表示）
- 生産者間のシナジーや相互作用で「組み合わせ」の戦略を生む
- 複数の投資パスで「どちらを先に取るか」のジレンマ
- コストカーブの調整で常に判断が面白い状態を維持

---

### Tiny Factory — コアコンセプト: 「流れの気持ちよさ」

**コア体験**: 自分が配置した機械とベルトで、素材がスムーズに流れて製品になっていくのを眺める満足感

**プレイヤーの感情**: 「おお、鉱石がベルトに乗って、精錬されて、ギアになって、出荷される…！全部繋がった！」

**主軸**: 生産ラインの「流れ」を視覚的に体感できること

**デザイン原則**:
- すべての演出は「流れが見える・感じられる」ために存在する
- アイテムの移動は滑らかに、目で追えるように表現する
- 生産完了・売却の瞬間には明確なフィードバックを返す
- 配置の自由度を確保し、「自分だけのライン」を作れる楽しさを守る
- 効率の追求はあくまで二次的な楽しみ — まず流れが気持ちいいことが最優先

**現状の課題**:
- アイテムの移動が1文字表現で流れが見えにくい
- ベルトのアニメーションが地味（方向記号の切り替えのみ）
- 生産完了や売却の瞬間にフィードバックがない
- ラインが「上手く流れている」ことの実感が薄い

**強化の方向性**:
- アイテムの流れを視覚的に表現する演出強化
- 生産完了・売却時のフィードバック（フラッシュ、ログ演出など）
- スループット可視化（「1秒に何個流れているか」）
- ベルト配置のUX改善（連続配置など）

---

### Everlight (常夜灯) — コアコンセプト: 「終わりのある夜を、何度でも」

**コア体験**: 出撃して上から迫る魔物を迎撃する緊張感そのものは維持しつつ、「今回の夜はどこまで進めば終わりなのか」を常に見える状態にする

**プレイヤーの感情**: 「あと1波で満月の魔王だ、届く…！」「Dawnが来た、次の夜はもっと長く過酷になる — もう一回いこう」

**主軸**: 迎撃アクションの手応え（維持）＋ 明確な区切り（Dawn）と手応えのあるエスカレーション（新しい敵・複数ボス）

**デザイン原則**:
- 「出撃して上から迫る敵を迎撃する」中核ループには手を入れない — 弱点は昔から
  「終わりがない/難易度の伸びが数値インフレだけ」という設計側の問題であって、
  中核アクション自体ではなかった
- 1回のVigilを「夜 (Night)」と呼び、ランクごとに明確な最終波
  (`logic::milestone_wave` = 15 + (rank-1)*5) と最終ボスを置く。最終ボスを
  倒すと「Dawn」を達成し、次のランクが解放される — 到達後もプレイは
  続行できるので、コアループを中断する強制終了は作らない
- 難易度の伸びは「数字が増えるだけ」にしない。waveが進むほど新しい敵種
  (狙撃者/甲殻兵/分裂体/突進者/散甲兵/極甲兵) が出現テーブルに合流し、
  ボスも魔王/影の魔女/大蛇/満月の魔王の4種をローテーションする — 同じ
  相手が大きくなるだけでなく、対応すべき状況そのものが増えていく
- 装甲系の弱点武器 (`EnemyKind::weak_to`) は敵種ごとに異なる (甲殻兵=光弾/
  散甲兵=散光/極甲兵=極光) — 1種の武器を伸ばすだけで全ての装甲系に
  対応できてしまうと武器の使い分けが生まれないため、意図的に分散させる
- 武器は光弾/散光/極光/光輪/流星の5種、対応する受動効果 (色を揃えた
  進化レシピのヒント) も5種で1:1対応させている。基本スロットは4つなので
  受動効果と同じく「拠点で枠を拡張しない限り全種は持てない」構図を武器
  側にも揃えた (`camp.extra_weapon_slot_level`)
- 進化 (武器+対応する受動効果) とは別に、武器"同士"の組み合わせにも
  シナジーを持たせる (光弾+極光「烙印」/散光+光輪「共鳴」/光輪+流星
  「増幅」)。効果自体は説明せず、揃った瞬間だけログで気配を残す —
  進化と同じ「点と点を線にする」設計をシナジーにも一貫させる
- ランクはwave内の難易度カーブとは別に基礎難易度・討伐報酬倍率を
  底上げする (`logic::wave_difficulty`/`ember_reward_mult`) — 高ランクは
  危険だが実入りも良い、というリスク/リターンで牽引力を作る
- `wave_difficulty` はそのランクの最終波 (`milestone_wave`) までは緩やかな
  線形 (+12%/波) で伸ばし、それを越えて夜番を続けた場合だけ指数関数的に
  跳ね上げる。恒久強化 (`camp.power_level` 等) には上限が無いため、線形
  のままだと「十分強化すればマイルストーンの先もいつまでも進める」状態
  になってしまう — 指数関数側が恒久強化の伸びを必ず追い越すことで、
  夜番そのものに終わりを作る
- 通常湧きは間隔短縮 (`spawn_interval_ticks`) に加えて同時湧き数
  (`regular_spawn_batch_size`) もwaveと共に増やす。間隔短縮だけでは
  第21波前後で頭打ちになるため、後半にかけて物量そのものが伸び続ける
  Vampire Survivors的な「敵を大量に薙ぎ払う」快感を後半の主軸に据える
- 次の目標は常に見える状態にする — 拠点画面に選択中ランクの目標
  (「第◯波『(ボス名)』を討伐する」) を常設表示する

**今後の課題**:
- `wave_difficulty` の指数関数側の係数 (`POST_MILESTONE_GROWTH_PER_WAVE`)
  と同時湧き数の上限 (`SWARM_MAX_BATCH`) はシミュレーターでの実測を元に
  決めた初期値であり、実プレイのフィードバックを踏まえた再調整の余地がある
- 恒久強化 (`camp.light_level`/`power_level`) 自体はまだ無制限に伸びるだけの
  %バフで、「極めた」達成感を伴う解禁型の強化に置き換える余地がある
- 満月の魔王/大蛇以降、ランクが伸びた時の最終ボスは2種のローテーションで
  賄っている。ランク帯がさらに伸びた場合の専用ボス追加は今後の検討事項
- 突進者 (`EnemyKind::Charger`) の加速倍率 (`CHARGER_BOOST_MULT`) や、
  流星の密集地点選定 (`pick_meteor_target` — レーン内で最も密集した1点を
  拾うだけの単純な貪欲法) は初期値であり、実プレイでの手応えを見て
  調整する余地がある
- 武器の組み合わせシナジーは5種10通りの組み合わせのうち3組
  (光弾+極光/散光+光輪/光輪+流星) だけを選んで実装している。残りの
  組み合わせ (光弾+散光、光弾+光輪等) にもシナジーを広げるかは、まず
  この3組の発見率・体感をシミュレーター/実プレイで見てから判断する
